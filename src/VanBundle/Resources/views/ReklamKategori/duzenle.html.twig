{% extends "base.html.twig" %}
{% block title %}Reklam Kategori Düzenle {% endblock title %}

 {% block body %}

     <header>
         <!-- Nav Başı -->
         <nav class="navbar navbar-inverse">
             <div class="container-fluid">
                 <div class="navbar-header">
                     <a class="navbar-brand" href="/">Van Kent Rehberi</a>
                 </div>
                 <ul class="nav navbar-nav">
                     <li class="active"><a href="{{ path('anasayfa') }}">Anasayfa</a></li>
                     <li><a href="{{path('emlak_listele')}}">Emlak</a></li>
                     <li><a href="{{path('seriilan_listele')}}">Seri İlanlar</a></li>
                     <li><a href="{{path('etkinlik_listele')}}">Etkinlikler</a></li>
                     <li><a href="{{path('yasam_listele')}}">Yaşam</a></li>
                     <li><a href="{{path('oto_listele')}}">Vasıta</a></li>
                     <li><a href="{{ path('reklam_listele') }}">Reklam</a> </li>
                     <li><a href="{{path('reklamKategori_listele')}}">Reklam Kategori</a></li>


                 </ul>
                 <ul class="nav navbar-nav navbar-right">
                     <!--      <li><a href="#"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>-->
                     <li><a href="{{ path('fos_user_security_logout') }}"><span class="glyphicon glyphicon-log-in"></span> Çıkış Yap</a></li>
                 </ul>
             </div>
         </nav>
         <!--Nav Sonu -->
     </header>

     <div class="page-header">
         <h2> <small> <h1>Kategori Düzenleme</h1></small></h2>
     </div>

     <!-- form div bası-->
     <div class="container">
     <form action="{{ path('reklamKategori_guncelle') }}" method="post">
         <div class="form-group">
             <label for="adi">Üst Kategori:</label>
         <select class="form-control" id="kategori" name="kategori">




             {% for kt in kategori %}

                 <option value="{{ kt.id }}" {{ (kt.id ==ReklamKategori.id  ) ? "<option selected='selected'> Kategori Seçiniz </option>" : '' }}> {{ kt.name }}  </option>

             {% endfor %}
         </select>

             </div>
         <div class="form-group">
             <label for="adi">Adı:</label>
             <input type="text" name="adi" id="adi" value="{{ ReklamKategori.name}}" class="form-control" required>
         </div>


         <input type="hidden" value="{{ ReklamKategori.id}}" name="id">


         <button type="submit" class="btn btn-default">Kaydet</button>
     </form>
     </div> <!-- form div sonu-->















     <footer>
         <p style="text-align: center">&copy; 2016  Putnas Kurumsal Yazılım Çözümleri</p>
     </footer>

 {% endblock %}
